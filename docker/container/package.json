{
  "name": "container-app",
  "version": "1.0.0",
  "description": "Container application that returns Hello World",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}